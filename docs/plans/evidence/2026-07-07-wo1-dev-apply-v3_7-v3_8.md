# WO-1 dev apply — V3_7 + V3_8 (proof)

**Binding:** TAO / `dev-subdomain` / branch `dev` · **Date:** 2026-07-07 · **Executor:** Kevin (HeidiSQL)
**Env:** dev — `new_development` / abod (`SELECT DATABASE()` = new_development, confirmed before each step).
**Prereqs met:** abod pane reconcile (Addendum B, V3_8 32-col PASS); dev master tables seeded (D-B closed).
**Scope:** V3_7 (columns/indexes/photo widen) + V3_8 (view rebuild). **V3_9 (FKs) NOT run — held per A2 until backfill validation.**

---

## V3_7 — contactdetails_tbl columns + indexes + photo widen — PASS
- `preflight` = "OK: contactdetails_tbl present in this schema"
- `new_cols_present_expect_12` = **12**
- `new_idx_present_expect_3` = **3**
- `contactPhoto` = **varchar / 500** (widened from 255, A1)

## V3_8 — contactdetails view rebuild (44-col INVOKER) — PASS
- `preflight` = "OK: view + base table + 12 new columns present -- rebuilding"
- `information_schema.views`: `security_type` = **INVOKER** (F19 convergence), `is_updatable` = YES
- `view_col_count_expect_44` = **44**
- smoke test (contactID 132418): value/pointer/sync cols = NULL; `contactPhone_src` = **'user'**
  (the ENUM `NOT NULL DEFAULT 'user'` backfilled existing rows correctly — user data wins over master)

**Window complete.** The dev `contactdetails` view now exposes all 44 columns as INVOKER; the SQL
body applied matched the reconciled file byte-for-byte (no re-emit needed).

---

## Not done / next (all gated)
- **V3_9 (3 FKs)** — held until the backfill WO populates + validates `master_co_contact_id`,
  `master_coid`, `company_location_id` (A2). Its pre-flight orphan/sentinel checks run then.
- **Backfill WO** — populate the 12 new columns from `contactitems` (hot fields, tie-break
  `primary_YN DESC, itemID ASC`) + master tables (now seeded on dev).
- **Prod promotion** — re-runs V3_8's own STOP-on-delta `SHOW CREATE VIEW contactdetails` safeguard;
  separate deploy authorization.
- **D-A** — dev `contactitems` view `root@108.185.100.195` DEFINER; INVOKER rebuild still open.
